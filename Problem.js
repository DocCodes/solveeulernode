class Problem {
  constructor (name, number, desc, prompt, solution) {
    this.name = name
    this.number = number
    this.desc = desc
    this.prompt = prompt
    this.solution = solution
  }
}

module.exports = Problem
